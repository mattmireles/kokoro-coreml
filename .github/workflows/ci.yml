name: ci

on:
  push:
  pull_request:

jobs:
  memory-health:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
      - run: bash scripts/check.sh
