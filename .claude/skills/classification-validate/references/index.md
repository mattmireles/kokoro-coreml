# Kokoro Decision-Validation References

Start with the validator that owns the record:

- `scripts/validate_f0_source_listening_decisions.py`
- `scripts/external_bakeoff/validate_listening_decisions.py`
- `tests/test_bakeoff_listen_quality_gate.py`
- `README/Plans/006-kokoro-external-bakeoff-plan.md`

Use `coreml-validate` for export or numerical parity rather than decision
labels.

